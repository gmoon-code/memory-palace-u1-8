# AP Biology Unit 8 F2 — Package QA

## Result

**PASS.** The cumulative Unit 8 F1 + F2 overlay is internally consistent and preserves the frozen F1 source-lock artifacts.

## Verified package properties

- All 18 files covered by the original F1 manifest remain byte-for-byte identical to the F1 release.
- All 255 canonical F1 records are classified exactly once at F2.
- 211 palace-managed records are assigned to 58 permanent loci, with 153 records embedded in governing loci.
- 13 practice-only records remain Challenge Lab functions.
- 31 scope guards remain non-runtime.
- The architecture contains 8 journeys, 22 bundles, 58 loci, and 40 mixed-discrimination sets.
- Every permanent locus specifies a scientific visual and stable spatial anchor.
- Exact-name requirements remain 135 and no mandatory spelling gate was introduced.
- No polished story, dialogue, audio-script, runtime Memory Object, or student Challenge Lab content is introduced.
- The F2 content-lock hashes the canonical catalog, F1 source lock, both F2 architecture files, F2 status, F2 release manifest, and architecture audit workbook.
- Source PDFs, `.git`, exact `.env`, Python bytecode, Python cache directories, pytest caches, and database files are excluded.
- The F2 application script retains the exact frozen Units 1–7 checksum guard.

- Final cumulative overlay archive contains **34 file entries**.

## Integration boundary

The artifact remains an overlay because the exact frozen Units 1–7 F6 source ZIP is not mounted in this runtime. The overlay can be applied directly to `MemoryPalace_V2_Mainline_Units1-7_Unit7-F6.zip` after its SHA-256 is verified as `d344f3641dd2949fc6824bc6e7d150b681b72511572669524be22af9984a36ed`.
